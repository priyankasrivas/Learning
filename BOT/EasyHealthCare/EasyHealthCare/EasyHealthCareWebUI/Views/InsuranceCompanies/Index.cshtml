@model IEnumerable<EasyHealthCareDataStore.InsuranceCompany>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.full_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.short_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.full_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.short_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.insurance_company_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.insurance_company_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.insurance_company_id })
        </td>
    </tr>
}

</table>
